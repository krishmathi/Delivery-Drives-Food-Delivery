html
  head
    title Delivery Drives
  body
    include ../partials/header

    h1 Welcome to the Delivery Drives Restaurants Page!
    each restaurant in restaurants
      a(href = "restaurants/" + restaurant.id) #{restaurant.name}
      br

    include ../partials/footer
